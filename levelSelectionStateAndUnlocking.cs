using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class levelSelectionStateAndUnlocking : MonoBehaviour
{
   private void Awake()
   {
     
   }

   private void Start()
   {
     // MainMenuManager.instance.LevelsLock_Unlock();
      Debug.LogError("HAPPENINGHERE",gameObject);   }
}
